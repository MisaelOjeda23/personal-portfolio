---
import Container from "./Container.astro";
import { projects } from "../projects";
---

<div id="projects">
  <Container>
    <div class="relative py-16 mt-28">
      <div
        aria-hidden="true"
        class="absolute inset-0 h-max w-full m-auto grid grid-cols-2 -space-x-52 opacity-40 dark:opacity-20"
      >
        <div
          class="blur-[106px] h-56 bg-gradient-to-br from-primary to-purple-400 dark:from-blue-700"
        >
        </div>
        <div
          class="blur-[106px] h-32 bg-gradient-to-r from-cyan-400 to-sky-300 dark:to-indigo-600"
        >
        </div>
      </div>

      <div class="mb-12 space-y-2 text-center">
        <h2
          class="text-3xl font-bold text-gray-800 md:text-5xl dark:text-white"
        >
          Mis proyectos
        </h2>
        <p class="lg:mx-auto lg:w-6/12 text-gray-600 dark:text-gray-300">
          Conoce un poco de mi trabajo y los proyecto que he desarrollado, cada
          uno de estos proyectos ha sido trabajado con mucho esfuerzo y tratando
          de crear las mejores interfaces para los usuarios.
        </p>
      </div>
    </div>
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {projects.map((project) => (
          <div class="group p-6 sm:p-8 rounded-3xl bg-white border border-gray-100 dark:shadow-none dark:border-gray-700 dark:bg-gray-800 bg-opacity-50 shadow-2xl shadow-gray-600/10">
            <div class="relative overflow-hidden rounded-xl">
              <img
                src={`/img/${project.img}`}
                alt="art cover"
                loading="lazy"
                width="1000"
                height="667"
                class="h-64 w-full object-cover object-top transition duration-500 group-hover:scale-105"
              />
            </div>
            <div class="mt-6 relative">
              <div>
                <h3 class="text-2xl flex items-center gap-1 font-semibold text-gray-800 dark:text-white">
                  {project.title}
                </h3>
                  <ul class="flex flex-wrap gap-2 mt-2">
                    {project.tags.map((tag) => (
                        <li class="items-center flex gap-1 bg-primary/10 px-3 py-1 rounded-full">
                          <img width="19" src={`${tag.svg}`} alt={tag.name} />  
                          <span class="text-xs font-semibold text-primary">{tag.name}</span>
                        </li>
                    ))}
                  </ul>
                <p class="mt-6 mb-8 text-gray-600 dark:text-gray-300">
                  {project.description}
                </p>
              </div>
              <div>
                <a class="inline-block" href={project.link}>
                  <span class="text-info text-primary">Ir a la web</span>
                </a>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </Container>
</div>
